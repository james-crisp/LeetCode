/**
 * Add two binary strings together and return the resultant 
 * binary string.
 * 
 * @author James Crisp
 */
class Solution {
    public String addBinary(String a, String b) {
        // Carry value
        int carry = 0;
        // Length of string a
        int len = a.length();
        // Set len to length of b if it's less than a
        if (b.length() < len) {
            len = b.length();
        }
        for (int i = 0; i < len; i++) {
            //
        }
    }
}